export interface PurchaseDTO{
    userId: string, 
    showId: string, 
    ticketId: string, 
    qtdPurchase:number
}

export interface Purchase{
    id: string,
    userId: string, 
    showId: string, 
    ticketId: string, 
    qtdPurchase:number
}