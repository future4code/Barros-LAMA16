export interface ShowDTO{
    weekDay: string,
    startTime: number, 
    endTime: number,
}

export interface Show{
    id: string
    weekDay: string,
    startTime: number, 
    endTime: number,
}